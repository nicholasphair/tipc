#include <assert.h>
#include "TipTypeOps.h"

Var *TipTypeOps::make_alpha(Var *var) {
    //if(TipVar * tv = dynamic_cast<TipVar *>(var)) {
    //    // Return tipalpha
    //} else if(TipVar * tv = dynamic_cast<TipVar *>(var)) {
    //    // Return tipalpha
    //} else{
    //    assert(0);
    //}
    return 0;
}

Mu *TipTypeOps::make_mu(Var *var, Term *term) {
    return nullptr;
}
